<resources>
    <!-- Base application theme. -->
    <style name="Theme.Wasted" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>
        <item name="checkboxStyle">@style/Theme.Wasted.Checkbox</item>

    </style>

    <style name="Theme.Wasted.ToggleButton" parent="Theme.Wasted">
        <item name="colorAccent">@color/green_500</item>
    </style>

    <style name="Theme.Wasted.NavMenu" parent="Theme.Wasted">
        <item name="android:listDivider">@android:color/transparent</item>
    </style>

    <style name="Theme.Wasted.Checkbox" parent="Widget.Material3.CompoundButton.CheckBox">
        <item name="colorOnSurface">@color/black</item>
        <item name="colorPrimary">@color/black</item>
    </style>

    <style name="Theme.Wasted.CustomSpinner" parent="android:Widget.Spinner">
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>